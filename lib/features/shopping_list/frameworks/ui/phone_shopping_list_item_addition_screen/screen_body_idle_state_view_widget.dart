import 'package:flutter/widgets.dart';

class ScreenBodyIdleStateViewWidget extends StatelessWidget {
  const ScreenBodyIdleStateViewWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
