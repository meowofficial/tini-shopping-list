import 'package:flutter/widgets.dart';

class ScreenIdleStateViewWidget extends StatelessWidget {
  const ScreenIdleStateViewWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
